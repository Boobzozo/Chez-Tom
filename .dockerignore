# Recompilés ou régénérés dans l'image.
node_modules
dist

# Données locales : elles ne doivent JAMAIS entrer dans l'image (le volume s'en charge).
chez-tom.db
chez-tom.db-shm
chez-tom.db-wal
uploads

# Secrets.
.env
.env.*
!.env.example

# Inutile à l'exécution.
.git
.gitignore
.claude
test
design-handoff
*.md
Dockerfile
.dockerignore
docker-compose*.yml
ecosystem.config.cjs
